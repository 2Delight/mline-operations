apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mline.fullname" . }}-test-connection"
  labels:
    {{- include "mline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
