apiVersion: v1
kind: Service
metadata:
  name: {{ include "mline.fullname" . }}
  labels:
    {{- include "mline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mline.selectorLabels" . | nindent 4 }}
